/**
 * Shared UI component types
 *
 * TODO: Add more shared types, e.g. size, variant, etc.
 */

/**
 * Severity for color-coding components like Button, Badge, etc.
 *
 *  TODO: Add more severities, e.g. secondary, info, light, dark, etc.
 * */
export type UISeverity = 'primary' | 'neutral' | 'success' | 'warning' | 'danger' | 'none'
